# PRD V2（终版）：跨部门 OpenCLAW 协作系统

**版本**：V2.0 · 终版  
**产品经理**：老高（PM）  
**日期**：2026-05-25  
**状态**：全员评审通过，待进入开发  

---

## 一、产品背景与目标

### 1.1 背景
中小型企业内部多部门使用 OpenCLAW 进行 AI 任务处理，但存在痛点：
- 各部门各自部署 OpenCLAW，模型重复采购，GPU 资源浪费
- 跨部门协作没有统一平台，任务流转靠口头/群聊，不可追踪
- 高价值大参数模型不是所有部门都买得起/跑得动
- 缺乏统一的任务进度管理和成果共享机制

### 1.2 目标
构建**轻量级、低成本、可插拔**的跨部门 OpenCLAW 协作平台，实现：
1. 主-分节点统一调度：任务下发 → 执行 → 回传全流程闭环
2. 模型统一管理，按部门按任务类型自动路由最优性价比模型
3. 任务进度实时可见，成果一键共享
4. **首月降本 30%**（模型调用共享池化，结果缓存降低重复计算）

### 1.3 目标用户

| 角色 | 典型用户 | 核心需求 |
|------|---------|---------|
| 系统管理员 | IT主管 | 节点管理、模型配置、数据看板、配额设置 |
| 部门主管 | 研发/市场/人事负责人 | 分配任务、看部门工单、看成本统计、锁定模型范围 |
| 普通员工 | 研发工程师/文案/HR | 领取任务、执行、提交成果、查看共享库 |

---

## 二、关键决策（全员评审结论）

### 决策 1：连接方案 → NATS + JetStream

**最终结论：NATS JetStream（全员一致通过）**

| 维度 | NATS | RabbitMQ | Redis Streams |
|------|------|----------|---------------|
| 二进制大小 | ~20MB | ~150MB + Erlang VM | 依赖 Redis（~30MB） |
| 内存占用 | 空闲 ~5MB | 空闲 ~50MB+ | 取决于用途 |
| 部署复杂度 | 单文件启动 | 需配 Erlang 环境 | 需额外装 Redis |
| 持久化 | ✅ JetStream | ✅ 完善 | ✅ RDB/AOF |
| ACK 机制 | ✅ at-least-once | ✅ 完善 | ✅ 需手写 |
| 前端 WS 原生支持 | ✅ 原生 | ❌ 需额外网关 | ❌ 需自己封装 |
| 运维成本 | 低 | 高 | 中 |

**⚠️ 强制配置（Reviewer 要求）：**
1. **必须开 JetStream**：NATS 默认内存模式，不开持久化主节点挂了队列全丢
2. **at-least-once + 手动 ACK**：分节点处理完才 ACK，避免消费中途挂掉任务消失
3. **前端对接**：NATS 原生支持 WebSocket，前端通过 `nats.ws` 库直接订阅 topic，不需要额外网关（Frontend 意见）

### 决策 2：服务器规格 → 4C8G 轻量云起步

**（Deploy 意见被采纳）**

MVP 阶段用 2C4G 当心 MQ + Web 面板 + 数据库三者同时跑时撑不住。**起步用 4C8G**（阿里云/腾讯云轻量云约 ¥200-300/月），后续上 Docker Compose 编排，扩缩容一条命令搞定。多花两三百，省得后面扩的时候要迁移。

### 决策 3：任务分配 → 手动领取为主 + 自动兜底

**全员结论：MVP 手动领取，但必须做三个机制保障**

1. **手动领取（MVP 核心）**：员工在 Web 面板上看到待领取任务列表，主动点击领取
2. **30 分钟超时自动释放（Reviewer + Tester 要求）**：员工领取后超过 30 分钟不点「开始处理」，任务自动回到待领取池，并记录异常日志
3. **自动兜底分配（Architect 要求）**：任务挂在那超过 10 分钟无人领取，后台自动分配给当前负载最低的 Agent。这个逻辑实际就是一条 SQL + 一个定时器，工作量不超过 2 天

**📐 数据库字段预留（Tester 要求）：** 任务表增加 `assignment_strategy` 字段（`manual` / `auto` / `hybrid`），明确每条任务被谁以什么方式分配，防止 V2 自动分配上线时与手动领取冲突。

### 决策 4：模型路由 → 三级模型选择机制

**最终方案（Architect + Reviewer + Tester 三方同意）：**

```
第一级（默认强制）：主节点自动路由，默认走最便宜的模型
  ├── 模型路由表按「任务类型 + 部门」自动匹配
  ├── 例如：文档摘要 → qwen2.5-7b（本地，¥0），代码审查 → deepseek-coder-7b（本地，¥0）
  └── 降本核心：默认不走贵模型，除非用户显式要求

第二级（员工可选覆盖）：提交/领取任务时可选「换模型」
  ├── 下拉框显示可用模型清单
  ├── 旁边显示成本对比：「推荐模型：¥0.03/次 │ 您选择：¥0.15/次」
  ├── 选了贵的，弹窗确认「成本警告」，但不强制拦截
  └── ⚠️ 后端记录每次模型选择日志，V2 用来训练自动路由算法

第三级（管理员锁）：部门主管可锁定某些任务的模型范围
  ├── 例：研发部普通代码审查锁死只用本地 7B，安全审计才允许调大模型 API
  └── 管理员可在 Web 面板一键切换全部门默认模型
```

**硬规矩（Reviewer 强调）：** 自动路由默认永远走**成本最低**的可用模型。只有任务里显式勾选了「需要高精度推理」，才走贵模型。不然降本就是口号。

**模型路由可追溯（Tester 要求）：** 每次 auto 路由决策记录日志：
```json
{
  "route_decision": {
    "task_id": "xxx",
    "input_type": "文档摘要",
    "recommended_model": "qwen2.5-7b",
    "reason": "任务类型=文档摘要，部门=市场部，默认路由到本地小模型",
    "cost_saved": "¥0.12"
  }
}
```

---

## 三、系统架构

### 3.1 部署架构

```
┌──────────────────────────────────────────────────────────────┐
│                      ☁️ 云服务器（4C8G）                       │
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │ 主 OpenCLAW   │  │  NATS JetStream│  │  MinIO 对象存储  │   │
│  │ (调度+路由+API)│←→│ (持久化消息队列)│←→│ (文件/模型/成果) │   │
│  ├──────────────┤  ├──────────────┤  ├──────────────────┤   │
│  │  Web 管理面板  │  │ PostgreSQL DB│  │  Redis (心跳+缓存) │   │
│  │  (React+AntD) │  │  (任务/用户)  │  │  (令牌桶+结果缓存) │   │
│  └──────────────┘  └──────────────┘  └──────────────────┘   │
└──────────────────────┬───────────────────────────────────────┘
                       │ WireGuard VPN / 公网 TLS
          ┌────────────┼────────────────┐
          │            │                │
  ┌───────▼────┐  ┌───▼────┐   ┌──────▼──────┐
  │ 研发部 K3s  │  │市场部 PC│   │ 人事部 PC   │
  │ ┌────────┐ │  │┌──────┐│   │┌───────────┐│
  │ │ 分Agent │ │  ││分Agent││   ││ 分Agent   ││
  │ │ +本地7B  │ │  ││+API直调││   ││+本地小模型 ││
  │ │ (量化)   │ │  ││(买token)│  ││ (量化4bit)││
  │ └────────┘ │  │└──────┘│   │└───────────┘│
  └────────────┘  └───────-┘   └──────────────┘
```

### 3.2 技术栈

| 组件 | 方案 | 理由 | 来源 |
|------|------|------|------|
| 主框架 | FastAPI (Python) | 异步、自动 OpenAPI、生态好 | PM |
| 消息队列 | NATS + JetStream | 20MB 二进制，持久化，WS 原生 | 全员一致 |
| 数据库 | PostgreSQL | 结构化数据 + JSONB | PM |
| 心跳/缓存 | Redis | 心跳写 Redis（TTL 60s），状态变更写 PG | Reviewer |
| 对象存储 | MinIO | S3 兼容，开源，轻量 | PM |
| 前端 | React + Ant Design Pro | 后台管理效率高 | PM + Frontend |
| 状态管理 | Zustand | 轻量，跨 Tab 共享 | Frontend |
| 移动端 | Ant Design Mobile + PWA | 管理层手机看板 | Frontend |
| 分节点 Agent | Python | 跨平台，批量回报 | PM |
| 编排 | Docker Compose → K3s | MVP 用 Compose，后续升 K3s | Deploy |

### 3.3 数据流与心跳设计（Reviewer 强调）

```
                    ┌───────────────────┐
                    │     Redis          │ ← 心跳写这里（TTL 60s）
                    │  (agent_heartbeat) │    50 员工 × 2880 次/天
                    └────────┬──────────┘     全部放 PG 会炸
                             │ 状态变更才写 PG
                    ┌────────▼──────────┐
                    │   PostgreSQL DB    │ ← 任务状态变更
                    │  (task_status_log) │    只有「待领取→处理中→已完成」
                    └───────────────────┘
```

**核心原则**：心跳数据高频低价值 → Redis；状态变更低频高价值 → PG。Redis 挂了还有 PG 兜底，反过来不成立。

---

## 四、功能模块

### 4.1 模块一：任务管理

| 功能 | 描述 | MVP | 来源 |
|------|------|-----|------|
| 创建任务 | 管理员/主管创建，选目标部门、模型、优先级 | ✅ | PM |
| 任务分配 | 自动按空闲度推荐 / 手动指定 | ✅ | PM |
| 任务领取 | 员工在 Web 面板看到待领取任务，一键接单 | ✅ | PM |
| 领取超时释放 | 领取后 30 分钟未开始，自动释放回池 | ✅ | Reviewer + Tester |
| 自动兜底分配 | 任务 10 分钟无人领 → 自动派给最空闲节点 | ✅ | Architect |
| 任务执行 | 员工本地 OpenCLAW 执行，Agent 上报进度 | ✅ | PM |
| 任务重试 | 失败自动重试（可配次数），超过进死信队列 | ✅ | Tester |
| 成果提交 | 上传文件 → 主管审核 → 归档 | ✅ | PM |
| 任务转派 | 转给另一部门/个人 | V2 | PM |
| 任务自动回收 | 领取后超 2 小时无活动，自动回收通知管理员 | ✅ | Tester |

**任务状态机（终版）：**
```
待分配 → 已领取 → 处理中 → 已完成
                     ↘ 失败 → 重试中 → 重试成功 → 已完成
                                       ↘ 已达上限 → 死信队列 → 人工介入
```

**领取超时逻辑：**
```
已领取状态持续 30 分钟 → 员工未点「开始处理」→ 自动释放回待分配
                                                     ↘ 记异常日志 (claim_timeout)
```

### 4.2 模块二：员工分节点 Agent

| 能力 | 说明 | MVP |
|------|------|-----|
| 连接主节点 | 启动时 WebSocket 注册，验证部门+身份 | ✅ |
| 心跳上报 | 每 30 秒上报存活 + 并发数 + 资源占用（→ Redis） | ✅ |
| 拉取任务 | 主动从 NATS 队列拉取分配给自己的任务 | ✅ |
| 资源下载 | 从 MinIO 下载 prompt 模板、参考文档等 | ✅ |
| 任务本地缓存 | Agent 拉到任务后存本地 SQLite，断网也能继续执行 | ✅ |
| 进度上报 | 每执行一步上报进度百分比 + 中间结果 | ✅ |
| 成果回传 | 执行完成上传 MinIO，返回 result_url | ✅ |
| 断网重连 | 恢复联网后批量回传结果 + 心跳 | ✅ |
| 自动更新 | 主节点下发新版本，自动拉取并重启 | V2 |

**连接状态机（Tester 要求）：**

```
Online ──→ Offline ──→ Reconnecting ──→ Online
  │                    │                  │
  │                   Stale (超时)        │
  │                                       │
  └───────────────────────────────────────┘
```

| 状态 | 行为 |
|------|------|
| Online | 正常心跳 + 拉任务 + 执行 |
| Offline | 本地缓存继续执行，不拉新任务 |
| Reconnecting | 尝试重连，最多 5 次 |
| Stale | 心跳超时超过 120 秒 → 主节点标记为失联 → 已分配任务自动释放 |

**Agent 离线交互模式（Web 面板）：**
- **Web 面板模式（MVP）**：员工浏览器打开 Web 面板，手动领取任务，看完任务详情后在自己本地的 OpenCLAW 执行，完成后在 Web 面板上传成果
- **Agent 自动化模式（V2）**：Agent 后台自动拉取、自动执行、自动提交，员工只用看结果

### 4.3 模块三：模型路由与管理

**模型注册表：**

| 任务类型 | 推荐模型 | 备用模型 | 部署位置 | 成本 |
|---------|---------|---------|---------|------|
| 代码审查 | deepseek-coder-7b (4bit) | gpt-4o-mini | 本地 | ¥0 |
| 文档摘要 | qwen2.5-7b (4bit) | gpt-4o-mini | 本地 | ¥0 |
| 文案生成 | gpt-4o-mini | doubao | API | ¥0.03/次 |
| 翻译 | qwen2.5-7b (4bit) | gpt-4o-mini | 本地 | ¥0 |
| 图像生成 | sd-xl-turbo | — | 本地 GPU | ¥0 |
| 深度推理 | gpt-4o / claude-3 | deepseek-r1 | API | ¥0.15/次 |

**默认路由永远走成本最低的模型（Reviewer 硬性红线）。**

**模型降级链：** 部门指定模型 → 如果超过成本阈值 → 降级到次优模型 → 如果也超 → 降级到最便宜模型

**模型路由缓存 Key（Architect 要求）：**
```
cache_key = sha256(task_type + model_name + input_hash + dept_id)
```
不能只用 task_type，必须对输入做 SHA256 匹配才命中缓存。

**模型配额硬限（Architect 要求）：**
- Redis Token Bucket，每个部门一个 bucket
- 管理员配置：`研发部 max_tokens/day = 10M` / `max_requests/day = 500`
- 超过直接返回 429，不依赖人工自觉
- 管理员可在 Web 面板一键切换全部门默认模型

### 4.4 模块四：成果共享

| 功能 | 描述 | MVP |
|------|------|-----|
| 一键分享 | 员工完成→点击「分享」→选同事/部门 | ✅ |
| 部门共享库 | 自动归档到部门共享 MinIO 目录 | ✅ |
| 跨部门提交 | 结果作为输入发新任务到另一部门队列 | V2 |
| 共享查看权限 | 按部门隔离，只能看到本部门共享库 | ✅ |

### 4.5 模块五：监控与运维

| 功能 | 描述 | MVP |
|------|------|-----|
| 任务看板 | 各部门待处理/处理中/完成数 | ✅ |
| Agent 状态 | 在线/离线 Agent 列表 | ✅ |
| 模型成本统计 | 各部门每日/每周模型调用费用 | ✅ |
| 灰度上线 | 先 10% 部门试用，观察失败率和延迟 | ✅ |
| Prometheus 指标 | 任务排队数、完成数、失败率、平均耗时 | ✅ |
| OpenTelemetry | 全链路 trace_id 追踪问题 | V2 |
| 压力测试 | 上线前必须过 | ✅ |
| 故障演练 | 模拟节点宕机、网络中断 | V2 |

---

## 五、错误场景与边界条件（Reviewer + Tester 强烈要求补充）

### 5.1 网络断开场景
| 场景 | 系统行为 |
|------|---------|
| Agent 断网 | 本地 SQLite 缓存继续执行，恢复联网后批量回传 |
| Agent 断网超过 120 秒 | 主节点标记 Stale，已分配任务自动释放 |
| 主节点 NATS 宕机 | JetStream 持久化保证消息不丢，重启后恢复消费 |
| 提交成果中断网 | 文件半上传 → MinIO 存储不完整 → Agent 重连后通过 task_id 校验完整性，重新上传 |

### 5.2 任务异常场景
| 场景 | 系统行为 |
|------|---------|
| 领取后 30 分钟未开始 | 自动释放回待分配池，记异常日志 |
| 领取后 2 小时无更新 | 自动回收 + 通知管理员 |
| 模型调用失败 | 自动降级到备用模型，日志记录降级链路 |
| 所有模型都失败 | 任务进死信队列，通知管理员人工处理 |
| 员工本地没装推荐模型 | Agent 检测 → 自动 fallback 到 API 模型 → 提示员工可手动安装本地模型 |

### 5.3 并发冲突场景
| 场景 | 措施 |
|------|------|
| 两个员工同时领取同一任务 | 分布式锁（Redis），保证一个成功 |
| 自动分配与手动领取冲突 | `assignment_strategy` 字段区分，互斥操作 |
| 任务重复提交 | 全局 task_id 去重，结果覆盖不重复计费 |

---

## 六、接口规范与幂等性（Tester 红线）

以下三个核心 API 必须支持幂等：

| 接口 | 幂等 Key | 说明 |
|------|---------|------|
| `POST /tasks/:id/claim` | `task_id + user_id` | 重复请求不会多次分配 |
| `POST /tasks/:id/submit` | `task_id + version` | 重复提交不会重复扣费 |
| `POST /tasks/:id/progress` | `task_id + progress_seq` | 进度上报幂等 |

所有接口返回值包含 `request_id`，便于追踪。

---

## 七、数据库核心表设计

### tasks 表
```sql
CREATE TABLE tasks (
    id UUID PRIMARY KEY,           -- ✅ 用 UUID 类型，不用 TEXT（Reviewer 意见）
    title TEXT NOT NULL,
    description TEXT,
    department_id TEXT NOT NULL,
    assignee_id TEXT,
    priority INTEGER DEFAULT 3,
    status TEXT DEFAULT 'pending',  -- pending/claimed/processing/completed/failed/dead
    assignment_strategy TEXT DEFAULT 'manual',  -- manual/auto/hybrid（Tester 要求）
    recommended_model TEXT,
    override_model TEXT,
    cache_key TEXT,
    result_url TEXT,
    created_at TIMESTAMP DEFAULT NOW(),
    claimed_at TIMESTAMP,
    started_at TIMESTAMP,
    completed_at TIMESTAMP,
    timeout_at TIMESTAMP,           -- 领取后 30 分钟自动释放的时间点
    retry_count INTEGER DEFAULT 0,
    max_retries INTEGER DEFAULT 3
);
```

### heartbeat_logs 表（仅记录状态变更）
```sql
CREATE TABLE heartbeat_logs (
    id BIGSERIAL PRIMARY KEY,
    agent_id TEXT NOT NULL,
    task_id UUID,
    status TEXT,
    progress INTEGER,
    created_at TIMESTAMP DEFAULT NOW()
);
-- 心跳高频数据走 Redis（TTL 60s），只有状态变更才写此表
```

### model_usage_logs 表（Tester 要求）
```sql
CREATE TABLE model_usage_logs (
    id BIGSERIAL PRIMARY KEY,
    task_id UUID NOT NULL,
    model_name TEXT NOT NULL,
    route_type TEXT,        -- auto / manual_override / admin_override
    usage_cost DECIMAL(10,4),
    input_tokens INTEGER,
    output_tokens INTEGER,
    response_time_ms INTEGER,
    success BOOLEAN,
    fallback_chain TEXT,    -- 降级链路日志
    created_at TIMESTAMP DEFAULT NOW()
);
```

---

## 八、前端交互设计要点

### 8.1 任务领取 UI（Frontend 方案）
- **待领取任务带呼吸灯提示**，有新任务时浏览器标题闪动 + Notification API 弹窗
- **抢单式领取**：点击后有 0.5s 确认动效（转圈→打勾），给用户「我抢到了」反馈
- **颜色标签**：红色（紧急）、黄色（普通）、灰色（低优先级）
- **模型选择下拉**：显示可选模型 + 预估耗时/费用对比
- **骨架屏**：加载中先 Skeleton 占位，不转菊花

### 8.2 实时更新
- 前端通过 NATS WebSocket 直接订阅任务 topic，状态变化实时推送
- UI 展示类似 CI/CD Pipeline 泳道图，无需手动刷新

### 8.3 移动端
- PWA 方案，管理员手机审批/查进度
- Service Worker 离线通知

---

## 九、开发时间线（14 天 MVP）

| 阶段 | 内容 | 后端 | 前端 | 天数 |
|------|------|------|------|------|
| 第 1-2 天 | 基础框架 + DB 表设计 + JWT 鉴权 | 阿强 | — | 2 |
| 第 2-3 天 | NATS + JetStream 集成 + 任务队列 | 阿强 | — | 2 |
| 第 3-5 天 | 任务 CRUD API + 领取/提交/完成 | 阿强 | 任务页面框架 | 3 |
| 第 5-6 天 | MinIO 集成 + 文件上传下载 | 阿强 | 成果上传组件 | 2 |
| 第 6-7 天 | 模型路由表 + 三级选择机制 | 阿强 | 模型下拉组件 | 2 |
| 第 7-8 天 | 进度上报 WebSocket + 心跳 Redis | 阿强 | 实时状态看板 | 2 |
| 第 8-9 天 | 领取超时 + 自动兜底分配 | 阿强 | — | 1 |
| 第 9-10 天 | 成果共享 + 部门共享库 | 阿强 | 共享页面 | 2 |
| 第 10-12 天 | 联调 + 灰度部署 1 个部门 | 全员 | 全员 | 3 |
| 第 12-14 天 | 压力测试 + 修复 + 全量上线 | 全员 | 全员 | 2 |
| **总计** | | **13 天** | **10 天** | **14 天** |

---

## 十、成本测算

### 10.1 固定成本

| 项目 | 月费用 | 说明 |
|------|--------|------|
| 云服务器 4C8G | ¥200-300 | 阿里云/腾讯云轻量级 ECS |
| MinIO 存储 100GB | ¥30 | 对象存储费用 |
| NATS 开源免费 | ¥0 | 开源 |
| 域名 + SSL | ¥20 | 每年 ¥200-300 |
| **合计** | **¥250-350/月** | |

### 10.2 可变成本（模型调用）

| 部门 | 使用模式 | 月预估费用 |
|------|---------|-----------|
| 研发部（10人） | 本地 7B 量化，免费 | ¥0 |
| 市场部（5人） | API 文案生成，约 500 次/月 | ¥15-25 |
| 人事部（3人） | 本地小模型 + API 少量 | ¥5-10 |
| **模型月费合计** | | **¥20-35** |

### 10.3 降本测算（对比各部门各自买 token）

| 场景 | 独立部署 | 统一平台 | 节省 |
|------|---------|---------|------|
| 算力 | 每部门各买 GPU | 共享本地模型池 | 60-70% |
| API 调用 | 各买各的 token | 集中采购按量分配 | 30-40% |
| 重复任务 | 无法利用他人结果 | 结果缓存命中 | 20-30% |
| **总降本** | | | **30-50%** |

---

## 十一、未完成文档清单（后续补充）

| 文档 | 责任人 | 计划时间 |
|------|--------|---------|
| API 详细定义 | 后端阿强 | 开发第 1 天 |
| 数据表完整 DDL | 后端阿强 | 开发第 1 天 |
| 前端组件树 + 页面清单 | 前端 | 开发第 1 天 |
| Docker Compose 编排文件 | Deploy | 开发第 3 天 |
| 压力测试方案 | Tester | 开发第 8 天 |
| 灰度上线检查清单 | 全员 | 开发第 10 天 |

---

## 十二、版本记录

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| V1.0 | 2026-05-25 | 初稿 |
| V2.0 | 2026-05-25 | 终版：全员意见全部整合 |

**审批人**：
- [ ] 高总（项目客户）
- [ ] PM（产品经理）
- [ ] Architect（架构师）
- [ ] Backend（后端）
- [ ] Frontend（前端）
- [ ] Tester（测试）
- [ ] Reviewer（代码守门员）
- [ ] Deploy（运维）
