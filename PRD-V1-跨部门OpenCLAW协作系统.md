# PRD V1：跨部门 OpenCLAW 协作系统

**版本**：V1.0  
**产品经理**：老高（PM）  
**日期**：2026-05-25  
**状态**：初稿待评审  

---

## 一、产品背景与目标

### 1.1 背景
中小型企业内部多部门使用 OpenCLAW 进行 AI 任务处理，但存在以下痛点：
- 各部门各自部署 OpenCLAW，模型重复采购，资源浪费
- 跨部门协作没有统一平台，任务流转靠口头/群聊，不可追踪
- 高价值模型（大参数 LLM）不是所有部门都买得起/跑得动
- 缺乏统一的任务进度管理和成果共享机制

### 1.2 目标
构建一个**轻量级、低成本、可插拔**的跨部门 OpenCLAW 协作平台，实现：
1. 主-分节点统一调度，任务下发 → 执行 → 回传全流程闭环
2. 模型统一管理，按部门按任务类型自动路由最合适模型
3. 任务进度实时可见，成果一键共享
4. **首月降本 30%**（模型调用不再各自为战，共享池化）

### 1.3 目标用户
| 角色 | 典型用户 | 核心需求 |
|------|---------|---------|
| 系统管理员 | IT主管 | 节点管理、模型配置、数据看板 |
| 部门主管 | 研发/市场/人事负责人 | 分配任务、看部门工单、看成本统计 |
| 普通员工 | 研发工程师/文案/HR | 领取任务、执行、提交成果、查看共享库 |

---

## 二、系统架构总览

### 2.1 部署架构

```
┌─────────────────────────────────────────────────────┐
│                    云服务器                          │
│  ┌─────────────┐  ┌──────────┐  ┌────────────────┐ │
│  │ 主 OpenCLAW  │  │ 消息队列  │  │ 对象存储(MinIO)│ │
│  │ (调度+路由)   │  │(NATS/MQ) │  │ (文件/模型仓库) │ │
│  ├─────────────┤  ├──────────┤  ├────────────────┤ │
│  │ Web 管理面板 │  │ API网关   │  │ 用户鉴权(JWT)  │ │
│  └─────────────┘  └──────────┘  └────────────────┘ │
└──────────────────────┬──────────────────────────────┘
                       │ 公网/WireGuard
          ┌────────────┼────────────┐
          │            │            │
  ┌───────▼────┐ ┌────▼────┐  ┌───▼────────┐
  │ 研发部K3s   │ │市场部PC  │  │人事部PC    │
  │ ┌────────┐ │ │┌──────┐ │  │┌─────────┐ │
  │ │分Agent │ │ ││分Agent│ │  ││分Agent  │ │
  │ │+本地7B  │ │ ││+API直调│ │  ││+本地小模│ │
  │ └────────┘ │ │└──────┘ │  │└─────────┘ │
  └────────────┘ └─────────┘  └────────────┘
```

**关键决策**：
- 主 OpenCLAW 部署在**云服务器**（阿里云/腾讯云轻量 ECS，2C4G 起步）
- 消息队列用 **NATS**（超轻量，Go 实现，资源占用极低，百万级消息/秒）
- 对象存储用 **MinIO**（S3 兼容，部署简单，用来存成果文件、模型文件、共享知识库）
- 分节点 Agent 用 **Python/Go 客户端**，启动时主动连接主节点 MQ

### 2.2 技术栈建议

| 组件 | 推荐方案 | 理由 |
|------|---------|------|
| 主框架 | FastAPI (Python) | 异步、自动生成 OpenAPI 文档、生态好 |
| 消息队列 | NATS | 超轻量（二进制 20MB）、Go 开发、支持集群 |
| 数据库 | PostgreSQL | 结构化数据+JSONB 灵活扩展 |
| 缓存 | Redis | 任务队列缓存、模型路由缓存、结果缓存 |
| 对象存储 | MinIO | S3 兼容、开源、部署简单 |
| 前端 | React + Ant Design | 组件丰富、后台管理类项目效率高 |
| 分节点 Agent | Python | 跨平台、生态好、与 OpenCLAW 天然兼容 |

---

## 三、核心功能模块

### 3.1 模块一：任务管理（核心）

**功能列表**：

| 功能 | 描述 | MVP |
|------|------|-----|
| 创建任务 | 管理员/部门主管创建任务，选择目标部门、模型、优先级 | ✅ |
| 任务分配 | 自动按空闲度分配 / 手动指定执行人 | ✅ |
| 任务领取 | 员工在 Web 面板看到待领取任务，一键接单 | ✅ |
| 任务执行 | 员工本地 OpenCLAW 执行任务，Agent 自动上报进度 | ✅ |
| 任务重试 | 失败任务自动重试（可配置次数），超过进死信队列 | ✅ |
| 任务归还 | 员工完成→提交成果→主管审核→归档 | ✅ |
| 任务转派 | 员工/主管将任务转给另一部门或个人 | V2 |

**任务状态机**：

```
待分配 → 已分配 → 处理中 → 已完成
                      ↘ 失败 → 重试中 → 处理中
                                        ↘ 已达上限 → 人工介入
```

**MVP 优先实现**：待分配 → 已分配 → 处理中（含进度上报）→ 已完成

### 3.2 模块二：员工 OpenCLAW 分节点 Agent

**部署方式**：员工在自己的机器上启动一个轻量 Agent 程序（命令行或系统托盘）

**Agent 的核心能力**：

| 能力 | 说明 |
|------|------|
| 连接主节点 | 启动时通过 WebSocket 向主节点 MQ 注册，验证部门+身份 |
| 心跳上报 | 每 30 秒上报存活状态 + 当前并发数 + CPU/内存/GPU 使用率 |
| 拉取任务 | 主动从任务队列拉取分配给自己的任务 |
| 资源下载 | 从 MinIO 下载任务所需的资源文件（prompt 模板、参考文档等） |
| 执行任务 | 调用本地 OpenCLAW 执行（或调用本地模型、或调用 API） |
| 进度上报 | 每执行一步上报进度百分比 + 中间结果（可选） |
| 成果回传 | 执行完成，把结果文件上传到 MinIO，返回 result_url |
| 自动更新 | Agent 版本更新时，从主节点自动拉取并重启 |

**员工交互方式**（两种模式）：

- **Web 模式（MVP）**：员工通过浏览器打开 Web 面板，手动领取任务，看到任务详情后在本地 OpenCLAW 执行，完成后在 Web 面板上传成果
- **Agent 模式（V2）**：Agent 后台自动拉取任务、自动执行、自动提交，员工只需查看结果

### 3.3 模块三：模型路由与管理

**模型注册表**：

```
主节点维护一个模型能力映射表
┌──────────┬─────────────────┬──────────┬──────────┐
│ 任务类型  │ 推荐模型         │ 备用模型  │ 部署位置 │
├──────────┼─────────────────┼──────────┼──────────┤
│ 代码审查  │ deepseek-coder-7b│ gpt-4o   │ 本地7B   │
│ 文档摘要  │ qwen2.5-7b       │ gpt-4o   │ 本地     │
│ 文案生成  │ gpt-4o-mini     │ doubao   │ API      │
│ 翻译      │ qwen2.5-7b       │ gpt-4o   │ 本地     │
│ 图像生成  │ sd-xl-turbo      │ —        │ 本地GPU  │
└──────────┴─────────────────┴──────────┴──────────┘
```

**模型降级策略（自动）**：

```
任务请求模型 A（如 gpt-4o）
  → 如果 A 超过部门成本月配额 → 自动降级到 B（gpt-4o-mini）
    → 如果 B 也超配额 → 自动降级到 C（本地 7B 模型）
      → 如果本地模型也忙 → 任务排队等待
```

**部门专属模型**：

每个部门可以在模型注册表上「盖戳」：
- 研发部：deepseek-coder-7b（本地封装）
- 市场部：gpt-4o-mini（API 调用）
- 人事部：qwen2.5-7b（本地封装）

主节点根据任务来源部门 + 任务类型，自动路由。

### 3.4 模块四：任务进度看板

**Web 面板核心视图**：

1. **我的任务**（个人视角）
   - 待领取、处理中、已完成三个 Tab
   - 每个任务显示：任务名、来源部门、优先级、截止时间、进度百分比

2. **部门看板**（部门主管视角）
   - 部门内所有员工的当前任务列表
   - 员工负载状态（空闲/忙碌/已满）
   - 部门今日完成量 / 平均处理时间 / 模型调用成本

3. **全局管理看板**（管理员视角）
   - 所有部门的状态总览
   - 每个节点在线/离线状态
   - 任务总量、完成量、排队数、失败率
   - 模型调用成本排行 Top 10

**进度可视化**：泳道图展示每个任务的生命周期，类似 CI/CD Pipeline，绿/黄/红色标识状态。

### 3.5 模块五：成果共享

**三种共享方式**：

1. **直接分享**（应用内）
   - 完成任务的成果页一键「分享」
   - 选择目标同事/部门
   - 对方收到通知 + 可直接查看/下载成果

2. **部门共享库**（自动归档）
   - 所有完成的任务成果自动归档到部门共享目录
   - 按时间/类型/关键词搜索
   - 支持点赞、评论、收藏

3. **跨部门协作转派**
   - 部门 A 的结果可以作为部门 B 的新任务输入
   - 例如：研发部生成代码 → 一键转给测试部验证

**共享内容**：
- 最终结果（文本/文件/图片）
- 使用的 prompt（可复用）
- 使用的模型 + 参数配置
- 执行耗时 + 成本（供参考）

---

## 四、交互流程详解

### 4.1 完整任务生命周期（用户视角）

```
管理员创建任务
  → 指定部门 + 模型偏好 + 上传/填写资源
  → 任务进入部门队列（员工 Web 面板看到待领取）
  → 员工领取任务
  → 员工查看任务详情（prompt、参考文档、期望格式）
  → 员工在本地 OpenCLAW 执行
  → 员工上传成果 + 填写执行日志
  → 部门主管审核（可选）
  → 归档到部门共享库
  → 成果可被分享给他人
```

### 4.2 员工抓取任务资源的流程

```
员工打开 Web 面板
  → 点击「待领取」Tab
  → 看到分配给部门的任务列表
  → 点击任务 → 进入详情页
  → 详情页展示：
     ├─ 任务描述 + 执行要求
     ├─ 参考文档/附件（可从 MinIO 下载）
     ├─ 推荐模型 + 备选模型
     ├─ 期望输出格式（模板）
     └─ 点击「领取」→ 任务状态变为「处理中」
```

### 4.3 员工上传成果的流程

```
员工在本地执行完成后
  → 回到 Web 面板 → 我的任务 → 处理中
  → 点击「提交成果」
  → 上传文件 / 粘贴文本
  → 填写：使用的模型、关键参数、备注
  → 点击提交
  → 系统自动：
       ├─ 文件上传到 MinIO
       ├─ 任务状态变为「待审核」/「已完成」
       ├─ 通知部门主管
       └─ 自动归档到部门共享库
```

### 4.4 主节点 Agent 自动交互流程（V2）

```
员工启动本地 Agent
  → Agent 自动连接主节点 MQ（WebSocket）
  → 上报身份、部门、本地模型列表
  → 主节点下发可执行任务列表
  → Agent 按优先级排队，逐个领取
  → Agent 下载任务资源（MinIO SDK）
  → Agent 调用本地 OpenCLAW API 执行
  → Agent 每 30 秒上报进度
  → Agent 执行完成 → 上传结果到 MinIO
  → Agent 上报完成事件 + result_url
  → 主节点更新 DB → Web 面板实时刷新
  → Agent 自动拉取下一个任务
```

---

## 五、数据库核心表设计（简化）

```
departments           -- 部门表
  ├ id, name, parent_id, model_config (JSON), budget_monthly

users                 -- 用户表
  ├ id, name, department_id, role, agent_token

tasks                 -- 任务表
  ├ id, title, description, department_id, assignee_id
  ├ status (pending/assigned/in_progress/completed/failed)
  ├ priority, task_type, model_preferred, model_actual
  ├ resource_urls (JSON), result_urls (JSON)
  ├ progress (0-100), started_at, completed_at
  ├ cost_tokens, cost_usd
  └ trace_id

task_logs             -- 任务日志表（可追溯每一步）
  ├ id, task_id, event, message, timestamp

models                -- 模型注册表
  ├ id, name, type, version, deploy_location (local/api)
  ├ supported_task_types (JSON), cost_per_1k_tokens

share_items           -- 共享成果表
  ├ id, task_id, user_id, department_id
  ├ visibility (private/dept/public)
  └ created_at
```

---

## 六、MVP 范围与版本规划

### V1.0 MVP（2周，14天）

| 模块 | 功能 | 工时 |
|------|------|------|
| 基础设施 | 云服务器部署、NATS、PostgreSQL、MinIO | 2天 |
| 主节点调度 | 任务CRUD、部门管理、任务分配（手动+自动） | 3天 |
| Web 面板 | 登录、我的任务、创建任务、提交成果、部门看板 | 4天 |
| Agent 基础 | 连接主节点、心跳上报、拉取任务列表 | 2天 |
| 模型路由 | 模型注册表、任务-模型自动匹配 | 2天 |
| 共享库 | 成果归档、直接分享 | 1天 |
| **合计** | | **14天** |

### V2.0 规划（后续）

- Agent 自动拉取+执行，无需人工看网页
- 任务结果缓存（相同输入直接返回缓存）
- 跨部门任务自动溢出（忙部门→闲部门）
- 成本统计大盘 + 月度预算预警
- 移动端 H5 适配

### V3.0 规划（远期）

- 部门间模型资源共享池
- AI 辅助的任务优先级智能推荐
- 与飞书/钉钉/企微集成（任务通知接入 IM）

---

## 七、降本增效测算

### 场景假设
- 公司 3 个部门，20 个员工，日均 100 次模型调用
- 原来各部门各自买 API Token 或各自部署 GPU

### 对比

| 项目 | 改造前（各自为战） | 改造后（平台共享） | 节省 |
|------|------------------|------------------|------|
| API 月费 | 3×¥2000 = ¥6000 | 统一接入 ≈ ¥2500 | ¥3500 |
| GPU 成本 | 3 台 GPU 机器（闲置） | 1 台 GPU 共享池 | 40%+ |
| 任务流转效率 | 电话/群聊沟通 | 系统自动流转 | ≈60% |
| 成果复用 | 不共享，重复造轮子 | 共享库，可搜索 | 时间节省 30% |

**预计首月总节省**：¥5,000-8,000，建设成本约 ¥2,000-3,000（云服务器+域名），**3 个月回本**。

---

## 八、风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| 员工不愿用新系统 | 高 | 中 | MVP 先让 2 个积极部门试用出标杆案例 |
| 不同部门模型偏好冲突 | 中 | 高 | 允许部门级模型配置覆盖，不强求统一 |
| 本地部署模型对员工机器要求高 | 中 | 高 | 支持纯 API 模式（员工机器不跑模型） |
| 数据安全隐私顾虑 | 中 | 高 | 任务数据隔离（部门间不可见），支持纯内网部署 |
| 云服务器成本超预算 | 低 | 中 | 初期用轻量云，按配置生成本打满再升级 |

---

## 九、附录：参考定价

### 云服务器
- 阿里云/腾讯云 2C4G 轻量云：¥68/月（新客首年更低）
- 4C8G：¥200/月
- MinIO 对象存储 100GB：¥30/月

### API 模型定价
- DeepSeek-Chat：¥0.06/千 tokens
- GLM-4-Flash（免费）：¥0/千 tokens
- Qwen-Lite：¥0.01/千 tokens

### 本地模型推荐
- Qwen2.5-7B（4-bit 量化）：4GB 显存，日常任务够用
- DeepSeek-Coder-7B（4-bit 量化）：4GB 显存，代码类任务
- 以上均可在没有 GPU 的机器上用 CPU + llama.cpp 跑（但会慢 5-10 倍）

---

*以上为 PRD V1 初稿，待团队评审后定稿进入开发。*
